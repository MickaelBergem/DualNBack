// The size of the grid
export const GRID_SIZE = 3;

// Time given to the user to give an answer
// export const DELAY_RESPONSE = 2500;
export const DELAY_RESPONSE = 1;

// Boundaries to change N
export const SCORE_UPGRADE = 0.90; // Jaeggi first experiment scores
export const SCORE_DOWNGRADE = 0.75;
export const MIN_LEVEL = 2; // Cannot drop below N=2
